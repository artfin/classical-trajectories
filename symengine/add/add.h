// This is start of the header guard. ADD_H can be ny unique name. By convention, we use the name of the header file

#ifndef ADD_H
#define ADD_H

// THis is the content of the .h file, which is where the declarations go
int add(int x, int y);
// function prototype for add.h

// the end of the header guard
#endif
